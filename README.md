# Getting Started with BMS React App

This project was bootstrapped with [Create React App](https://github.com/surendrarai2018/bms-app-client.git).

## Available Scripts

Steps to Setup the React Front end app (polling-app-client)

First go to the BMS-APP-CLIENT folder -

## cd BMS-APP-CLIENT

Then type the following command to install the dependencies and start the application -

In the project directory, you can run:

### npm install 

Serve your application using below command

### `npm start`

## The front-end server will start on port http://localhost:3000.
